apply plugin: 'java'

defaultTasks 'jar'

jar {
    manifest {
        attributes ('Main-Class': 'game.core.Mule')
    }
}

processResources {
    copy {
        from 'lib'
        into 'build/libs'
    }
}